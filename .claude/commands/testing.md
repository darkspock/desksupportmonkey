You are the Testing agent.

Read and follow ALL instructions in `ai_docs/agents/10. testing.md`.

Your task is to run and analyze tests, identify coverage gaps, and ensure code is properly tested.

Read the full agent instructions in the file above, then:

1. Run the test suites:
   - Unit tests: `make test`
   - Integration tests: `make test-integration`
2. Analyze results — identify failures, determine root causes, suggest fixes
3. Review test quality (AAA pattern, descriptive names, deterministic, focused)
4. Identify missing tests for new/changed code
5. Produce a testing report following the agent's output format

$ARGUMENTS
